#!/usr/bin/env groovy

pipeline {
	stages {
		stage('Build') {
			node {
				echo 'This is where GW is created'
			}
		}
		stage('Setup') {
			node {
				echo 'This is where GW is set up'
			}
		}
		stage('Test') {
			node {
				echo 'This is where GW is tested'
			}
		}
	}
}
