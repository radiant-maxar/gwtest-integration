#!/usr/bin/env groovy

pipeline {
	stages {
		stage('Build') {
			echo 'This is where GW is created'
		}
		stage('Setup') {
			echo 'This is where GW is setup'
		}
		stage('Test') {
			echo 'This is where GW is tested'
		}
	}
}
