#!/usr/bin/env groovy

pipeline {
	stages {
		stage('Build') {
			sh "echo 'This is where GW is created'"
		}
		stage('Setup') {
			sh "echo 'This is where GW is setup'"
		}
		stage('Test') {
			sh "echo 'This is where GW is tested'"
		}
	}
}
